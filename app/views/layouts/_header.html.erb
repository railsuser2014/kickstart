 <div class="pull-right">
    <% if session[:user_id] %>
     <%= link_to "Logout", logout_path, method: :delete, class:"btn btn-info" %>
     <% else %>
   <%= link_to "Login", login_path, class:"btn btn-info" %>
   <% end %>
  </div>
<div class="page-header">
  <h1> Kick Start </h1>
</div>